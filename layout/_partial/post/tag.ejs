<% if (post.tags && post.tags.length){ %>
    <div class="article-tag tagcloud clearfix">
        <%- list_tags(post.tags, {
            show_count: false,
            class: 'article-tag'
          }) %>
    </div>
<% } %>